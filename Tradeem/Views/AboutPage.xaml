<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Tradeem.Views.AboutPage" 
             xmlns:vm="clr-namespace:Tradeem.ViewModels">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Image Source="TradeEm.png"/>
            <Button Margin="20,10,20,0" Text="Sign Up" Command="{Binding SignUpCommand}" TextColor="Black" />
            <Button Margin="20,10,20,0" Text="Sign In" Command="{Binding OpenWebCommand}" TextColor="Black" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>